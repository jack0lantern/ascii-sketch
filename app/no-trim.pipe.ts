// import { Pipe, PipeTransform } from '@angular/core';

//  * Prevents the trim (default) of the input
//  * Usage:
//  *   value | noTrim
//  * Example:
//  *   {{ 2 |  exponentialStrength:10}}
//  *   formats to: 1024

// @Pipe({name: 'noTrim'})
// export class NoTrimPipe implements PipeTransform {
//   transform: function(value: string): string {
//     return value;
//   }
// }